.planning/

# Rust build artifacts (Cargo.lock is committed — this is a binary crate)
/target

# .planning/ is intentionally local-only: GSD workflow artifacts, never published.

# Release artifacts generated at publish time (uploaded as gh release assets, not committed)
/SHA256SUMS.txt
/RELEASE_NOTES.md
